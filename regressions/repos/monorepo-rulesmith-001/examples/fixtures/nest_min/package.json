{
  "name": "nest_min",
  "private": true,
  "scripts": {
    "build": "nest build",
    "test": "vitest run",
    "lint": "eslint .",
    "format": "prettier --write .",
    "dev": "nest start --watch"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
