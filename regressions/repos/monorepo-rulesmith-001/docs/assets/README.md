# Assets

Recommended quickstart demo file:

- `rulesmith-quickstart.gif`

Suggested content (about 45-60 seconds):
1. clone + install + build
2. run `rulesmith start` on a target repo
3. show generated diff/apply flow

Update the image link in root `README.md` to this local file once recorded.
