# AGENTS.md

Use `spatie/laravel-permission` for permissions.
Expose AI routes under `/ai`.
Use `Http/Controllers/{Module}/` for placeholders.
Generated logs live in `storage/logs/error.log`.
Run tests with --config configs/missing.json.
Use src/example.ts as an example.
Read `docs/missing.md` before changes.
Review `System/SchoolController.php` inside the module section.
