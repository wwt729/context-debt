{
  "mcpServers": {
    "filesystem-docs": {
      "description": "Restricted to repository docs for local browsing",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "docs"],
      "roots": ["docs"]
    }
  }
}
