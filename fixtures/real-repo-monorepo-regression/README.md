# Example Monorepo

Install `@acme/toolkit` in local examples.
Expose the app under `/api`.
Use `src/{module}/index.ts` as a placeholder in documentation templates.
See [Architecture](./docs/architecture.md).
Check `packages/shared/src/config.ts` before changing shared defaults.
