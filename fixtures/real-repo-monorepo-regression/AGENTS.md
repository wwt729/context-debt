# Agent Guide

Use pnpm for all commands.
Run `pnpm test` before merge.
Read `docs/agent-playbook.md` before touching release flows.
Open `packages/cli/src/index.ts` when changing the CLI surface.
Review `.github/workflows/ci.yml` before editing CI behavior.
