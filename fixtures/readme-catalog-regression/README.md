# Regression Fixture

This tool supports repositories with files such as:

- `AGENTS.md`
- `.github/copilot-instructions.md`
- `.cursor/mcp.json`

`context-debt` currently scans project metadata and instruction-like files, including:

- `CLAUDE.md`
- `.claude/mcp.json`

Read `docs/testing.md` before changes.
See [Spec](./docs/spec.md).
