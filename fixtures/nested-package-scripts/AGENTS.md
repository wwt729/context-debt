# Workspace Guide

## Frontend

- Located in `frontend`
- Run tests with `npm run test`
- Build production assets with `npm run build`

## Extension

- Located in `tools/extension`
- Run linting with `npm run lint`
- Run tests with `npm run test`
- Build release assets with `cd tools/extension && npm run build`
